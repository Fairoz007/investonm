// This file is intentionally left minimal. The app now always uses the
// default purple theme defined in the global CSS variables, and the
// interactive theme switcher UI has been removed.

export {};
